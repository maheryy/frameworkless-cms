sidebar-main:
  dashboard:
    label: Tableau de bord
    icon: fas fa-columns
    route: /admin/dashboard
  page:
    label: Pages
    icon: far fa-file
    route: /admin/pages
    sub-links:
      list_page:
        label: Liste pages
        route: /admin/pages
      new_page:
        label: Ajouter page
        route: /admin/new-page
  appearance:
    label: Apparance
    icon: fas fa-palette
    route: /admin/navigations
    sub-links:
      navigations:
        label: Navigations
        route: /admin/navigations
      new_navigation:
        label: Ajouter nav
        route: /admin/new-navigation
      themes:
        label: Thèmes
        route: /admin/themes
  user:
    label: Utilisateurs
    icon: fas fa-users
    route: /admin/users
    sub-links:
      list_user:
        label: Liste utilisateurs
        route: /admin/users
      new_user:
        label: Ajouter utilisateur
        route: /admin/new-user
      current_user:
        label: Profil
        route:
      roles:
        label: Roles
        route: /admin/roles
  appearance:
    label: Apparance
    icon: fas fa-palette
    route: /admin/themes
    sub-links:
      themes:
        label: Thèmes
        route: /admin/themes
      navigations:
        label: Navigation
        route: /admin/navigations

sidebar-bottom:
  settings:
    label: Paramètres
    icon: fas fa-cog
    route: